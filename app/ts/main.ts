import {bootstrap}    from 'angular2/platform/browser'; // This is found in the node module - imported
import {AppComponent} from './app.component'; // ./ means relative path - look in the same directory as im in now (our components we created)

bootstrap(AppComponent);
